{% extends "admin/MainAdmin.html.twig" %}

{% block content %}
<div class="table-responsive">
    <table class="table table-striped table-sm">
    <thead>
        <tr>
        <th>Intitule</th>
        <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for capacite in capacites %}
        <tr>
            <td>{{ capacite.intitule }}</td>
            <td><a href="{{ path('admin_capacite',{ 'action': 'edit' , 'capacite': capacite.id }) }}"><i class="fas fa-pencil-alt"></i></a> <a href="{{ path('admin_capacite',{ 'action': 'del' , 'capacite': capacite.id }) }}"><i class="fa fa-trash" ></i></a></td>
        </tr>    
        {% endfor %}
    </tbody>
    </table>
</div>
{% if form is defined %}
<hr>
{{ form_start(form) }}

  <div class="form-group">
    <label for="exampleInputEmail1">Intitule :</label>
 {{ form_widget(form.intitule, {'attr': {'class': 'form-control'}}) }}
  </div>
  
  <button type="submit" class="btn btn-success">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
</div>
{% else %}
<a class="btn tbn primary" href="{{ path('admin_capacite',{ 'action': 'add' }) }}">Creer une nouvelle Capacite</a>
{% endif %}
{% endblock %}