{% extends "admin/MainAdmin.html.twig" %}

{% block content %}
<div class="table-responsive">
    <table class="table table-striped table-sm">
    <thead>
        <tr>
        <th>Nom</th>
        <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for niveau in niveaux %}
        <tr>
            <td>{{ niveau.nom}}</td>
            <td><a href="{{ path('admin_niveau',{ 'action': 'edit' , 'niveau': niveau.id }) }}"><i class="fas fa-pencil-alt"></i></a> <a href="{{ path('admin_niveau',{ 'action': 'del' , 'niveau': niveau.id }) }}"><i class="fa fa-trash" ></i></a></td>
        </tr>    
        {% endfor %}
    </tbody>
    </table>
</div>
{% if form is defined %}
<hr>
{{ form_start(form) }}

    <div class="form-group">
    <label for="exampleInputEmail1">Nom :</label>
 {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
 </div>
  <button type="submit" class="btn btn-success">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
</div>
{% else %}
<a class="btn tbn primary" href="{{ path('admin_niveau',{ 'action': 'add' }) }}">Creer un Nouveau Niveau</a>
{% endif %}
{% endblock %}