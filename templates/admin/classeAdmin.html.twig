{% extends "admin/MainAdmin.html.twig" %}

{% block content %}
<div class="table-responsive">
    <table class="table table-striped table-sm">
    <thead>
        <tr>
        <th>Nom</th>
        <th>Annee</th>
        <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for classe in classes %}
        <tr>
            <td>{{ classe.nom_classe }}</td>
            <td>{{ classe.annee }}</td>
            <td><a href="{{ path('admin_classe',{ 'action': 'edit' , 'classe': classe.id }) }}"><i class="fas fa-pencil-alt"></i></a> <a href="{{ path('admin_classe',{ 'action': 'del' , 'classe': classe.id }) }}"><i class="fa fa-trash" ></i></a></td>
        </tr>    
        {% endfor %}
    </tbody>
    </table>
</div>
{% if form is defined %}
<hr>
{{ form_start(form) }}

    <div class="form-group">
    <label for="exampleInputEmail1">Nom :</label>
 {{ form_widget(form.nom_classe, {'attr': {'class': 'form-control'}}) }}
 </div>
  <div class="form-group">
    <label for="exampleInputEmail1">Annee :</label>
 {{ form_widget(form.annee, {'attr': {'class': 'form-control'}}) }}
  </div>
  <button type="submit" class="btn btn-success">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
</div>
{% else %}
<a class="btn tbn primary" href="{{ path('admin_classe',{ 'action': 'add' }) }}">Creer une nouvelle Classe</a>
{% endif %}
{% endblock %}